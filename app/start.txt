win: 
t0 - Detector
cd ~\Desktop\project\TJK-UAV
.\app\run_detector.bat --det sam3 --target moli3

t1 - Tracker
cd ~\Desktop\project\TJK-UAV
.\app\run_tracker.bat

uav: OWL v22
# Set photography.autofocus: false in app/agent/config/v22.yaml.
t0 - bridge
cd ~/TJK-UAV
# First use only. bash app/robot/owl_ego/build.sh # First use only.
bash app/run_owl_ego.sh bridge

t1 - server
cd ~/TJK-UAV
bash app/run_owl_ego.sh server

t2 - Console
cd ~/TJK-UAV
bash app/run_owl_ego_console.sh
init
takeoff
wait

t3 - Agent
cd ~/TJK-UAV
bash app/run_agent_v22.sh --detector-host  192.168.2.194

uav: i7 v22
t-build - first use or after planner source changes
cd ~/TJK-UAV
bash app/robot/i7/build.sh
bash app/run_i7.sh check

t0 - hardware (MAVROS, LiDAR, FAST-LIO2, RTSP relay; no old planner/nav)
cd ~/TJK-UAV
bash app/run_i7.sh hardware

t1 - sensors (RTSP receipt timestamps; stationary detection)
cd ~/TJK-UAV
bash app/run_i7.sh sensors

t-check - ground checks after t0 and t1
cd ~/TJK-UAV
bash app/run_i7.sh check --live
# Resolve every reported error before starting bridge/server.
# Check flight_gates and the live sensor results.

t2 - bridge and independent EGO
cd ~/TJK-UAV
bash app/run_i7.sh bridge

t3 - Robot server
cd ~/TJK-UAV
bash app/run_i7.sh server

t4 - Console (keep open after takeoff)
cd ~/TJK-UAV
bash app/run_i7.sh console
health
pose

# Flight stage only, after on-vehicle verification.
# init/takeoff require flight_enabled, sensors_validated and failsafe_validated.
# Select POSITION on RC before init, not after it.
# A latched takeover requires explicit init on disarmed, stopped POSITION ground state.
init
takeoff
# Wait for WAIT_OFFBOARD, then select OFFBOARD on RC.
# The program then requests arming and climbs to the configured height.
wait

t5 - Agent
cd ~/TJK-UAV
bash app/run_agent_v22.sh --detector-host 192.168.31.66
