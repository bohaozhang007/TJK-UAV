# TJK-UAV 双端协作规则

## 角色与边界

- Agent 端：Windows，运行 Agent v21、SAM3、SAM2、DA3；主要维护 `src/agent/`、
  `src/robot_client/`、视觉服务和 Agent 启动脚本。
- Robot 端：OWL 本机，运行 ROS/MAVROS/FAST-LIO/独立 EGO 与 HTTP；主要维护
  `src/robot/`、`ros/owl_nav/`、Robot 启动脚本及对应测试。
- 开始工作先依据用户上下文确认自身角色，阅读接口契约、双方状态和最新编号消息。
  不以共享仓库路径或“本机”一词猜测角色；本次初始化方为 Robot 端。
- 不代改另一端实现或状态。需要对端配合时，在己方编号消息中写清具体接口/行为、
  原因、验证办法。用户明确授权跨端修改时，以用户指令为准。

## 协作文件

- `docs/owl_ego_contract.md` 是唯一接口契约。修改须说明已有行为、变更行为、
  双方实现状态、兼容策略；单端实现不代表双端已联调。未收到对端确认，不宣称完成。
- `docs/collab/agent_status.md` 仅 Agent 端更新；`robot_status.md` 仅 Robot 端更新。
  初次搭建可创建空的对端文件，不代写进度或验收结论。
- `docs/collab/messages/agent-NNN.md` 由 Agent 发出；`robot-NNN.md` 由 Robot 发出。
  按双方实际对话轮次配对编号：agent-001 → robot-001 → agent-002 → robot-002。
  同一轮的本地讨论、排查、实现和交接补充汇总到当前己方消息，不按主题或修改次数
  新建编号；收到对端下一轮消息后，再使用对应新编号回复。
  消息列日期、发送方、回复对象、结论、变更、验证和待回复项。已完成往返的历史
  正文保留，后续修正在新一轮说明；用户明确要求合并整理时可调整，文档迁移可修复链接。
- 用户要求的历史消息整理可以由任一端完成，保留原文并说明来源。
- 有保留价值的排查与实现说明直接放入己方编号消息，注明记录时点；handoff 使用
  编号回复，不单设文档。不建立 archive/notes、日期快照、校验清单或重复跳转页。
- status 记录当前配置假设、已完成/待完成、测试证据、阻塞项和最新消息链接。
  历史排查消息不能用旧结论覆盖更新的用户决定。

## 已确认任务与几何假设

- 用户确认的联合调试流程（2026-09-11）：Robot本机console init/takeoff后保持打开，
  Agent在起飞停稳后直接接入，无需用户手动交接；console stop悬停和land降落均可抢占Agent运动。
  起飞由console执行；Agent仍可在有效会话内主动降落（含任务正常结束），两端均可land。
  内部仍保持一个有效运控租约，本机操作员权限不得替Agent续心跳或绕过遥控接管。
- v21 与旧 owl/v20 并存。Robot 导航不经 Captain，避障用现有 FAST-LIO 机载点云；
  Agent DA3 用于目标感知，不作为经 Wi-Fi 传输的避障输入。
- 用户允许相机位置近似等于无人机位置，并确认云台角度固定。最新用户决定忽略安装俯仰，
  当前按相对机体水平朝前（pitch=0°）近似；不代表实际云台已调整为水平。
- 用户允许近似内参及中心主点。图像尺寸不能唯一确定 fx/fy；采用的 FOV 或焦距
  比例必须显式配置和标注。不得把估计内参、零安装平移或未知畸变写成实测标定。
- 已标定模式与近似模式明确区分。未真实完成畸变校正的图像不得谎报 `rectified:true`；
  Agent 对近似模式的显式接纳及日志记录须按契约补充条款联调。

## 修改与验证

- 保留用户已有改动；不擅自回滚、覆盖厂家工作空间或升级/替换现场依赖。
- 图像、状态、心跳、取消保持并发；5 s 机载租约、实际停止确认、任务轨迹隔离、
  定位 epoch、人工接管与 PX4 failsafe 不因视觉近似而削弱。
- 默认只读检查；测试使用 mock 或独立 ROS master。未经用户明确授权不实飞、
  不自动停厂家服务、不自动解锁。不把 mock FCU/真实 EGO 联调称为 PX4 SITL。
- 用户已授权换电池重启后的脚本控制权交接：`run_owl_ego.sh prepare` 和 `bridge`
  可在新鲜状态确认 connected、disarmed、ON_GROUND 后自动停止 `/captain` 与
  `/mavros_controller`，再复查发布者。该授权不包含停止其他厂家节点、修改开机项或
  自动解锁/实飞；`check` 继续只读。该已授权流程不必每次另行询问。
- 按改动运行针对性测试并记录实际结果；不把此前测试当作新增路径已经验证。
- 文件回复不等于已经发送给对端，不自动通过外部消息工具发送或发布。
